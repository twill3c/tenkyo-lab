# TEST_SPEC.md — tenkyo-lab

<!-- scaffold template v1.13.0 から展開(2026-08-29) -->

## 実行規約

- `npm test`(vitest)を stage 3–5 の判定に使用する。L2 で Python の参照実装(G-04 の照合相手)が
  入った時点で `python -m pytest -q` を併走させ、`.wt/gate.json` の `test_command` を両方に更新する
- 生成物(`data/chunks.json` / `data/oracle.json`)に依存するケースは `describe.skipIf` で囲む。
  生成物は `.gitignore` 済みで、`npm run fetch && npm run chunks && npm run oracle` で作る
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。**SPEC の保証粒度を超える期待値を書かない** |
| 外部権威(e-Gov 法令データ) | 法令 ID と取得日をフィクスチャの先頭に書く |
| 実測 | **実測日と実測値**をコメントに残す |

件数・行数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。

**閾値が集合の大きさに依存する判定は、合成フィクスチャでテストしない**(HC-057)。
「全体の N%」「上位 k 件」のように閾値がデータ量の関数である関数は、フィクスチャを小さくすると
判定条件そのものが別物になる(総数 13,600 で 68 件の閾値が、総数 302 では 1.5 件になった)。
統計量を引数として直接与える単体試験で境界を見る(T-314/T-315)。

**どちらの門で落ちたかを名指しで確かめる。** 「落ちた」だけを見ると、意図と違う門で落ちていても
緑になる(T-312 は `queryTooShort` が 0 で `notSubstantive` が 1 であることを別々に確かめる)。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| `tests/fixtures/mini-law.json` | 手作り。実物(著作権法 345AC0000000048, 2026-08-29)のタグ構成を写した | 構造の対照 |
| `data/raw/*.json` | e-Gov 法令 API v2(PDL1.0)。26 法令 | 実データ |
| `data/oracle.json` | 条文の内部参照から自動生成(SPEC §2.2) | 検索の正解 |
| G-07 検分シート | 上記から種つき無作為抽出 100 問 → 人が目視 | **計器の検査** |

**循環の禁止**: オラクルの選別条件はクエリ単独の性質だけで組む。正解の条文と語が重なるかで
選別してはならない。この規律自体を T-302〜T-307 の不変量で見張る。

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-101 | F-01 | 本則の条だけを採り、附則は件数だけ数える | 本則 2 条 / 附則 1 条 |
| T-102 | F-01 | 条番号(ArticleTitle)を本文に含めない | 本文に「第一条」が無い |
| T-103 | F-02 | 項を分けて持ち、号は項本文に連なる。ItemTitle は混ぜない | 項 1,2 / 号本文あり |
| T-104 | F-01 | 章の見出しを文脈として保持 | 「第一章　総則」 |
| T-105 | — | 走査対象が空でない | 条・項ともに 1 件以上 |
| T-106 | F-01 | 漢数字と Num 属性の相互変換(枝番を含む) | 第四条の二 ⇄ 4_2 |
| T-107 | F-01 | 実データ(著作権法)の条数が実測値と一致 | 本則 239 / 附則 150 |
| T-201 | G-01 | **陽性対照**: 実物に現れる参照表現をすべて捕まえる | 7 例すべて解決 |
| T-202 | G-01 | **陰性対照**: 条番号単体・相対参照・他法令参照を拾わない | 8 例すべて 0 件 |
| T-203 | G-01 | 一文の複数参照を出現順に拾う | [17, 89] |
| T-204 | G-01 | 拾った範囲が原文と一致(位置ずれなし) | slice 一致 |
| T-205 | §2.2 | 相対参照を種類ごとに数える | 前条/前項/次条/同条 各 1 |
| T-206 | §2.2 | **陰性対照**: 相対参照が無ければ 0 件 | 合計 0 |
| T-207 | §2.2 | 参照文字列だけが消え、他は 1 文字も変わらない | 完全一致 |
| T-208 | §2.2 | 複数参照でも位置ずれしない(後ろから消す) | 完全一致 |
| T-209 | §2.2 | **陽性対照**: 除去後に条番号表現が残らない | 0 件 |
| T-210 | §2.2 | **陰性対照**: 参照が無ければ 1 文字も変わらない | 恒等 |
| T-301 | — | 走査対象が空でない | チャンク・問ともに 1 件以上 |
| T-302 | §2.2 | 循環の禁止1: クエリに条番号表現が残らない | 該当 0 件 |
| T-303 | §2.2 | 循環の禁止2: 出題元の除外指定が全問にある | 欠落 0 件 |
| T-304 | §2.2 | 循環の禁止3: 正解に出題元が混ざらない | 該当 0 件 |
| T-305 | §2.2 | 正解はすべて索引に実在する | 宙づり 0 件 |
| T-306 | §2.2 | 正解は同一法令内を指す | 越境 0 件 |
| T-307 | §2.2 | 自条への参照が正解にならない | 該当 0 件 |
| T-308 | — | 文の分割は句点を残し、連結すると元に戻る | 恒等 |
| T-309 | §2.2 | 実質文字数は記号と括弧を数えない | 17(実測) |
| T-310 | §2.2 | バイグラムは記号をまたがない | 3 個 |
| T-311 | §2.2 | **陽性対照**: 識別力のある語を伴う参照は課題になる | 1 問生成 |
| T-312 | §2.2 | **陰性対照**: 実質文字数不足で落ちる(門を名指し) | notSubstantive=1 |
| T-313 | §2.2 | **陰性対照**: 列挙条文は参照件数・被覆率で落ちる | 0 問 |
| T-314 | §2.2 | 識別力の判定は文書頻度を直接与えて確かめる(HC-057) | 0 個 / 3 個以上 |
| T-315 | §2.2 | **陰性対照**: 未知の語は df 0 = 最も稀として数える | 1 個以上 |
