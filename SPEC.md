# SPEC.md — tenkyo-lab(典拠ラボ)

<!-- scaffold template v1.13.0 から展開(2026-08-29)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

RAG(Retrieval-Augmented Generation)のパイプラインを**段ごとに開いて、つまみを回して、
その場で測れる**実験台。作るのは「RAG を使った何か」ではなく「RAG そのものを解剖する台」である。

RAG の成否は生成ではなく検索側で決まる、という主張を**実測で示す**ことを狙う。
したがって主役は検索・評価であり、生成は差し替え可能な最終段として扱う。

閲覧者の端末で走り、サーバ関数を持たない。所有者側に課金される経路を作らない。

## 2. 素材とオラクル

### 2.1 コーパス

e-Gov 法令データから主要法令を採録し、**条・項単位**でチャンク化する。

| 項目 | 内容 |
|---|---|
| 対象 | 主要法令 26 本(憲法・民法・刑法・会社法・労基法・著作権法・個人情報保護法 ほか)。要求 26 / 取得 26 |
| 粒度 | 条の項。1 項 = 1 チャンク |
| 規模 | **6,239 条 / 13,600 チャンク**(実測 2026-08-29)。当初の見積り 6,000 チャンクは倍以上外していた |
| 索引本文の長さ | 中央値 92 字 / p90 235 字 / 最大 4,101 字 / 総計 1,764,905 字(実測) |
| 附則 | **採らない**。改正経過にひもづく時限規定が多く参照構造が本則と別物になる。除外 2,131 条(計上済み) |
| 権利 | 公共データ利用規約(第1.0版)= PDL1.0。出典記載と**加工した旨の記載**が義務。`data/PROVENANCE.md` |

法令の選定は題名からの推定で確定しない(HC-012)。API で題名を検索し、**完全一致した場合のみ**
law_id を採る。0 件・複数件は採らず理由つきで報告する(`scripts/fetch-laws.mjs`)。

### 2.2 検索オラクル(本プロジェクトの背骨)

法令の条文には内部参照が大量に書かれている(「第五条第二項の規定を準用する」等)。
**参照を含む文をクエリ、参照先の条文を正解**とすることで、人手ゼロ・非循環の検索評価セットが得られる。

**循環の禁止は三重に置く**(`scripts/build-oracle.mjs`)。

1. クエリから参照文字列そのもの(「第五条第二項」)を除去する。除去しないと疎検索が文字列一致で当たる
2. **出題元のチャンクを検索結果から除外する。** クエリは出題元の本文の一部なので、
   除外しないと出題元が必ず 1 位に来る(それは正解ではない)。各問に `excludeFromResults` を持たせる
3. 自条への参照を捨てる

**計器を先に疑う**(kototoi-do G-01 の教訓)。100 問を無作為抽出して人が目視し、
**この検分(G-07)を通るまで G-01 の閾値判定を行わない**。

**除外は黙って捨てず、理由ごとに数える**(実測 2026-08-29):

| 理由 | 件数 |
|---|---|
| 他法令への参照 | 1,114 |
| 参照被覆率が高い(列挙条文) | 833 |
| 一文の参照が 4 件以上(列挙条文) | 710 |
| 実質文字数が 25 字未満 | 183 |
| 識別力のある語が残らない | 94 |
| クエリが 20 字未満 | 69 |
| 自条への参照 | 24 |
| 参照先が本法令に存在しない | 22 |
| **採用** | **2,943** |

相対参照(前項 3,317 / 前条 1,355 / 同項 1,456 ほか計 8,827)は解決に文脈が要るため**使わない**。
件数のみ記録する。

**列挙条文の除外は G-07 の一度目で分かった。** 罰則・読替・準用の列挙は文のほとんどが参照で
できており、マスクすると「）若しくは（において準用する場合を含む。」しか残らない。人にも答えられない。
除外条件は**クエリ単独の性質**(参照被覆率・参照件数・実質文字数・識別力)だけで組む。
「正解の条文と語が重なるか」で選別してはならない —— それは答えを問いに埋め込む循環である。

### 2.3 生成の素材

- 既定は**抽出型**(検索結果から典拠つきの回答を組み立てる)。LLM を積まない
- **事前生成の見本 20 問**をビルド時に Claude で作成し同梱する。鍵なしでも「生成つき RAG の完成形」が見える
- 見本は**評価に使わない**。見本を正解として指標を計算することを禁ずる(循環の禁止)

## 3. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 質問を入力すると、チャンク→埋め込み→検索→再ランク→文脈→回答の**全段の中身**が展開されて出る | must |
| F-02 | 分割戦略 3〜4 通りの索引を切り替え、同じ条文がどう切れるかを実物で比較できる | must |
| F-03 | 密検索(e5-small)/ 疎検索(文字 bi-gram BM25)/ ハイブリッド(RRF)を切り替えられる | must |
| F-04 | k・密疎の配合・RRF の定数・MMR の λ・文脈トークン予算をつまみで変えられる | must |
| F-05 | つまみを変えると Recall@k / MRR / nDCG@10 が**端末上で即時再計算**される | must |
| F-06 | 評価セットの内訳(生成数・除外理由別件数・目視検分の結果)を画面に出す | must |
| F-07 | 抽出型の回答が、引用元の条番号と該当箇所のハイライトつきで出る | must |
| F-08 | 事前生成の見本 20 問を「生成つきの完成形」として閲覧できる(評価には使わない旨を明記) | should |
| F-09 | 「効いた・効かなかった」の実測記録ページ(測って捨てたものを含む) | must |
| F-10 | パイプラインの実装解説ページ | should |
| F-11 | `query:` / `passage:` 前置詞を外した索引も配り、外すと何が壊れるかを体験できる | should |

## 4. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | サーバ関数ゼロ。Next.js `output: "export"` で静的配信のみ | ビルド成果物に関数が無いことを検査 |
| N-02 | 所有者側に課金される経路を持たない。API キーを要する箇所が既定の経路に無い | 依存とネットワーク送信先の走査 |
| N-03 | トップページは索引を 0 バイトしか取得しない | 実ブラウザでネットワークを実測 |
| N-04 | 初回の一問で取得する総バイト数を**画面に出す**。数字は実測値であり見積りではない | 実ブラウザで範囲つき GET により実測 |
| N-05 | 二問目以降の追加取得は 1MB 未満 | 同上 |
| N-06 | 評価の再計算(数千問 × 上位 k)は端末上で 1 秒未満 | 実ブラウザで計測 |
| N-07 | 日本語本文にキリル文字等の同形異字が混入しない | 文字種検査(senoto-mori text-hygiene 準拠) |

## 5. 較正ゲート

**閾値は測る前に置き、以後動かさない。** 落ちたゲートに対して最初に疑うのは実装ではなく計器である。

| ID | 内容 | 閾値 | 性格 |
|---|---|---|---|
| G-01 | 参照オラクルによる Recall@10(密検索・条の項単位) | **0.50** | 主ゲート |
| G-02 | 陰性対照(乱数語・他言語・無関係語)の最高スコアが、正しいクエリの最高スコアの中央値を下回る | 下回る | 対照 |
| G-08 | 疎検索(BM25)が正解を上位 10 に 1 件も入れられない問題の割合 | **10% 以上** | 偏りの検算 |
| G-09 | 密検索の陽性対照(チャンク本文で問うと、**同一本文のチャンク**が 1 位) | 100% | 対照 |
| G-10 | 語彙を刈った模型と刈る前の模型で、日本語クエリの埋め込みのコサイン | **0.9999 以上** | 保存 |
| G-11 | 初回の一問で取得する総バイト数(実ブラウザ実測) | **50MB 以下** | 配布量 |
| G-12 | 二問目以降に追加で取得するバイト数(実ブラウザ実測) | **1MB 未満** | 配布量 |
| G-13 | 詰めた索引から復元した検索結果が、詰める前と完全一致する | 100% | 照合 |
| G-03 | 疎検索の陽性対照(チャンク本文で問うと、**同一本文のチャンク**が 1 位) | 100% | 対照 |
| G-04 | 二実装照合。TypeScript の検索と Python の参照実装で上位 k の順位が一致 | 100% | 照合 |
| G-05 | int8 量子化索引と float32 索引で、上位 10 件の**集合**が一致する問いの割合 | **95%** | 保存 |
| G-06 | ハイブリッド(RRF)が密・疎の単独を下回らない | 下回らない | ※ 下記 |
| G-07 | 評価セットの目視検分(無作為 100 問)で正解が妥当と判定される割合 | **90%** | 計器検査 |

**G-07 の結果(2026-08-29・loop_001)**

| 回 | 種 | ○ 妥当 | △ 保留 | × 不可 | 妥当率 | 判定 |
|---|---|---|---|---|---|---|
| 一度目 | 20260829 | — | — | 15 | **85%** | 不合格 → 列挙条文の除外を追加 |
| 二度目 | 20260830 | 91 | 8 | 1 | **91%** | **合格**(△ を不可に数えた厳しい側で) |

閾値 90% は、一度目の自動フラグ件数(13/100)を見た後・人手判定を見る前に置いた。以後動かしていない。

**残る弱点は読替規定**(「〜とあるのは〜とする」)で、二度目の △ 8 件すべてがこの型。
マスク後に読替の断片だけが残り、課題として痩せる。**隠さず画面に出す**(SPEC §6)。

**閾値の根拠(loop_002 の冒頭・2026-08-29 に、何も測る前に置いた)**

- **G-01 = 0.50** —— 13,600 チャンクから 10 件返して、参照先の条文を**半数以上**引けること。
  無作為なら 10/13,600 ≒ 0.0007。半数を「検索が効いている」の最低線と定める。
  **定義**: Recall@10 = 各問について |上位10 ∩ 正解| / |正解| を取り、全問で平均(マクロ平均)。
  正解が複数ある問題は、全部引けて 1.0 になる。
  補助指標として Recall@1 / @5 / @20・Hit@10・MRR・nDCG@10 も出すが、**合否は Recall@10 だけで決める**
- **G-08 = 10% 以上** —— 語彙的に引けない問題が 1 割を切ったら、疎検索に有利な評価セットを
  作ってしまったことになり、G-06(ハイブリッド対単独)の判定が信用できなくなる。
  **定義を loop_002 の途中で直した**(2026-08-29・何も測る前)。当初の「バイグラムの重なりが
  下位 1% の問題が 10% 以上」は、下位 1% が定義上 1% しかないため成立しない文だった。
  重なりの代理指標をやめ、**疎検索そのものが解けない問題の割合**で測る。閾値 10% は動かしていない

**落ちたときに閾値を動かさない。** 最初に疑うのは計器(索引・評価器・オラクル)であり、
それでも落ちるなら**落ちたまま出荷して理由を書く**(SPEC §6)。

**G-03 は一度落ちた。閾値ではなく対照の書き方が間違っていた**(2026-08-29 / loop_002)。
当初は「そのチャンクが 1 位」と書いていたが、**本文が完全に重複するチャンクが 439 件**
(異なり 156 種・うち「削除」だけが 71 件)あり、原理的に満たせない対照だった。
外れた 10 件はすべてスコアがビット一致し、同点が id で割れていた。
BM25 の実装は正しい。**閾値 1.0 は動かさず、対照を「同一本文のチャンクが 1 位」に言い直した。**

> 重複の副作用として、**正解の本文が他チャンクと重複する問いが 29 件(1.0%)**ある。
> 検索が「本文は正しいが id が違う」チャンクを返すと外れと数えるので、
> Recall は最大 1.0 ポイント過小に出る。補正しない —— 補正するとオラクルが検索に寄る。

**閾値の根拠(loop_003 の冒頭・2026-08-29 に、何も測る前に置いた)**

- **G-05 = 95%** —— kototoi-do が同じ模型・同じ量子化法で 98.2% を出している(閾値 95%)。
  同条件なので同じ線を引く。**順位そのものの一致ではなく上位 10 件の集合一致**で測る ——
  配布量を 4 分の 1 にする代わりに、上位の顔ぶれが変わらなければ検索としては保存されている。
  あわせて Recall@10 の劣化幅も出すが、**合否は集合一致率だけで決める**
- **G-04 = 100%** —— 順位は完全一致を要求する。許容を置かない(HC-054)。
  ただし **`log` のような超越関数は処理系で 1 ULP 違いうる**ので、
  食い違ったときは (a) 順位が入れ替わったのか (b) スコアの下位ビットだけか、を分けて報告する
- **G-06** —— ハイブリッド(RRF)の Recall@10 が、密・疎いずれの単独も下回らないこと。
  **破れることを許す**(下記)

**閾値の根拠(loop_004 の冒頭・2026-08-29 に、何も測る前に置いた)**

- **G-10 = 0.9999** —— kototoi-do の G-06 と同じ模型・同じ刈り方なので同じ線を引く。
  **借り元の測り方まで写す**(HC-060): 借り元は「日本語クエリ 16 件すべてでコサイン 0.9999 以上」で、
  16 × 1.0 = 16 と整数になるので**件数の判定**である。当方も件数で判定する。
  **1 問ずつ通して測る** —— 束にすると詰め物の長さが食い違い、模型の差でないものが差に見える
- **G-11 = 50MB** —— kototoi-do の実測は 48.8MB(索引 7.3 / 模型 29.8+語彙 1.0 / ort 10.6)。
  当方は索引を int8 に落として 5.0MB なので、同等以下に収まるはず。**収まらなければ落ちる**
- **G-12 = 1MB** —— 二問目以降に必要なのは条文の一節だけである

**G-09 は loop_002 の途中で足した。** G-01 が落ちた原因を切り分けるためである。
G-03 は疎検索の対照しか見ておらず、**密検索には対照が無かった** —— 検出系に対照を対で置く
規律(HC-041)の穴だった。G-01 の値には触れていない。ここで見るのは索引の健全性だけである。

**G-08 は G-07 の副作用を検算する。** 識別力フィルタ(§2.2)はコーパスの語の希少さだけを見るので
正解を覗いてはいないが、**語彙的に引ける問題へ寄る**恐れは残る。クエリと正解が 1 文字のバイグラムも
共有しない問題 —— 密検索でしか解けない問題 —— が十分に残っていることを、L2 で測って確かめる。
残っていなければ、疎検索に有利な評価セットを作ってしまったことになり、G-06 の判定が信用できない。

### 5.1 loop_002 の実測(2026-08-29)

コーパス 13,600 チャンク / 問い 2,808 件 / 模型 multilingual-e5-small(q8)。

**チャンク単位(条の項)— G-01 はこの行で判定する**

| 手法 | Recall@1 | Recall@5 | **Recall@10** | Recall@20 | Hit@10 | MRR | nDCG@10 |
|---|---|---|---|---|---|---|---|
| 疎(BM25) | 0.0558 | 0.2139 | 0.3374 | 0.4711 | 0.3821 | 0.1595 | 0.1835 |
| 密(e5-small) | 0.0589 | 0.2248 | **0.3327** | 0.4508 | 0.3722 | 0.1610 | 0.1845 |
| ハイブリッド(RRF) | 0.0641 | 0.2474 | **0.3740** | 0.4986 | 0.4184 | 0.1779 | 0.2064 |

**条単位に丸めた場合(粒度の寄与を分けるための診断。ゲートには使わない)**

| 手法 | Recall@10 | 粒度の寄与 |
|---|---|---|
| 疎 | 0.4909 | +0.1535 |
| 密 | 0.4938 | +0.1611 |
| ハイブリッド | **0.5375** | +0.1635 |

| ゲート | 実測 | 閾値 | 判定 |
|---|---|---|---|
| G-01 密検索 Recall@10 | **0.3327** | 0.50 | **×** |
| G-02 陰性対照 | 陰性最高 0.8428 / 正例最高の中央値 0.9221 | 陰性 < 正例中央値 | ○ |
| G-03 疎検索の陽性対照 | 0.9960(504 件中 2 件外し) | 1.0 | **×** |
| G-08 疎検索が解けない問題の割合 | 0.6179 | 10% 以上 | ○ |
| G-09 密検索の陽性対照 | 1.0000(300 件中 0 件外し) | 1.0 | ○ |

**G-01 は落ちたまま出荷する。閾値は動かさない。** 計器の側は次のとおり切り分けた:

1. ベクトルのノルムは全 13,600 行で 1.000(最小 0.999999 / 最大 1.000001)、零ベクトル 0 行
2. **G-09 密検索の陽性対照 300/300**。索引・接頭辞・並びは壊れていない
3. 索引とチャンク、クエリとオラクルの id の並びを実行時に照合している
4. 条単位に丸めると 0.4938(+0.1611)。粒度の寄与を分離しても 0.50 に届かない

**この不合格は成果物である。** 同じコーパス・同じ模型でも、粒度と融合を変えるだけで
0.3327 → 0.5375 と **61% 相対で動く**。つまみが指標を動かすことを、失敗したゲートが実証している。
「効いた・効かなかった」(F-09)の中心に据える。

### 5.3 loop_003 の実測(2026-08-29)

| ゲート | 実測 | 閾値 | 判定 |
|---|---|---|---|
| G-04 二実装照合 | **全層で完全一致** | 100% | ○ |
| G-05 量子化の順位保存(上位10の集合完全一致) | **0.8725** | 0.95 | **×** |
| G-06 ハイブリッドが単独を下回らない | 0.3740 対 0.3374 | 下回らない | ○ |

**G-04 は三層で照合した。** 上の層が壊れていれば下の層の一致は意味を持たない。

| 層 | 対象 | 結果 |
|---|---|---|
| 1 分かち書き | 全 13,600 チャンク + 全 2,808 クエリのバイグラム(sha256) | 完全一致 |
| 2 疎索引 | N / avgdl / 語数 12万余 / 文書長総和 / 語の並び / df 全語 | 完全一致 |
| 3 検索順位 | 疎 200 問・密 20 問・融合 20 問の上位 20 件 | 完全一致 |
| 3 スコア | 密検索の最上位スコア | **ビット一致** |

`log` の 1 ULP 差を懸念していたが、実測では順位もスコアも割れなかった。
Python 側は **numpy の dot を使わず**、float32 を float64 に広げた逐次和で JS と演算順を揃えている(HC-054)。

**G-05 は落ちたまま出荷する。閾値は動かさない。**

| 測り方 | 次元ごと μ/s | 陰性対照(全体一律) |
|---|---|---|
| 上位10の**集合完全一致**(G-05 の定義) | **0.8725** | 0.7521 |
| 上位10の**重なり比率の平均** | 0.9872 | 0.9741 |
| 1 位が同じ | 0.9811 | — |
| Recall@10 | 0.3318(f32 は 0.3327) | 0.3313 |

配布量は **19.9MB → 5.0MB + 変換表 15KB(25.1%)**。

**閾値 0.95 は kototoi-do の実測 98.2% から借りたが、同じ文言で測り方が違っていた。**
借り元は「1 問ごとの重なり比率 \|A∩B\|/k の平均」であり、当方は「集合の完全一致率」で測っている。
後者は必ず厳しい。気づいた手がかりは算術で、借り元は 40 問で 98.2% と書いており
40 × 0.982 = 39.28 は整数にならない —— 「該当した問いの割合」ならなるはずである(HC-060)。
**借り元と同じ定義で測ると 0.9872 でほぼ一致する。** 技術は効いている。
それでも**定義を借り元に合わせ直すことはしない** —— 事後の閾値変更と区別がつかなくなるため。

### 5.5 loop_004 の実測(2026-08-30)—— 配布物

| ゲート | 実測 | 閾値 | 判定 |
|---|---|---|---|
| G-10 語彙刈り込みの保存 | **16/16**(最悪 0.999999) | 全件 0.9999 以上 | ○ |
| G-13 詰めた索引の照合 | **完全一致** | 100% | ○ |
| G-11 初回一問の総取得量 | 48.44MB(**ファイル実測。実ブラウザ実測は L5**) | 50MB 以下 | 保留 |
| G-12 二問目以降 | 数十 KB 見込み(**実ブラウザ実測は L5**) | 1MB 未満 | 保留 |

**模型の刈り込み**(kototoi-do の手順を流用)

| | 刈る前 | 刈った後 |
|---|---|---|
| 語彙 | 250,002 語 | **23,421 語(9.4%)** |
| 模型 | 112.8MB | **29.8MB** |
| 語彙表(tokenizer.json) | 16.3MB | **1.0MB** |

刈り方は文字種基準(日本語 + 2 文字以下の ASCII + 名指しで残す 263 トークン)なので、
**コーパスに依らない**。kototoi-do と同じ 23,421 語になった。
G-10 の検証クエリは**作り話ではなく実際のオラクルから種つきで 16 問抽出**した。
刈った範囲(ロシア語 0.757 / 英語 0.860)がきちんと落ちていることも同時に見える。

**配布物と段取り**

| 段取り | 中身 | 生 | gzip |
|---|---|---|---|
| トップページ | **索引を 1 バイトも取らない**(N-03) | 0 | 0 |
| 一問目(密検索) | `vec_i8.bin` 4.98 + `meta.json` 0.19 + `quant.json` 0.01 | **5.19MB** | 4.44MB |
| つまみ(疎・ハイブリッド) | `postings.bin` 2.21 + `terms.bin` 0.10 + 他 0.13 | **2.44MB** | 1.26MB |
| 結果表示 | `text.bin`(全 5.22MB)から **HTTP Range で必要な一節だけ** | 数 KB | — |
| 模型一式 | 刈った ONNX + 語彙表 | **30.86MB** | — |
| 実行系 | 非 jsep 版 ort(jsep 版は 24.89MB) | **12.39MB** | — |

**一問目の合計 48.44MB**(模型 30.86 + ort 12.39 + 索引 5.19)。

**配布量はつまみごとに分けて画面に出す。** 「ハイブリッドにすると 2.44MB 増える」ことを、
使う側が見て選べるようにする —— 配布量そのものが RAG の設計判断だからである。

**ポスティングは差分 + 可変長整数で詰めた。** 1,080,280 件を素朴に uint32+uint8 で持つと 5.2MB だが、
文書番号を昇順に並べて差分を取ると **2.21MB(gzip 1.08MB)**になる。

### 5.4 「ハイブリッドは常に勝つ」が破れる条件(F-09)

RRF の定数 k を 1〜1000 まで振っても、**全体では一度も単独に負けなかった**(最良は k=10 の 0.3782。
慣例の k=60 は 0.3740 で最良ではない)。

**しかし法令別に分けると 17 法令中 2 法令で融合が単独に負ける。**

| 法令 | 密 | 疎 | 融合 | 差 |
|---|---|---|---|---|
| 個人情報の保護に関する法律(n=106) | **0.392** | 0.325 | 0.358 | **−0.033** |
| 民事訴訟法(n=162) | 0.254 | **0.344** | 0.341 | −0.003 |
| 不正競争防止法(n=34) | 0.382 | 0.412 | **0.515** | +0.103 |

**片方が明らかに強いとき、RRF はそれを平均して引き下げる。** 個人情報保護法では密検索が疎検索を
7 ポイント上回っており、融合すると密の単独に 3 ポイント負ける。
「ハイブリッドにしておけば安全」は全体平均では正しく、**部分集合では正しくない**。

### 5.2 通説の検証(loop_002 時点)

| 通説 | 実測 | 判定 |
|---|---|---|
| 密検索は疎検索(BM25)に勝つ | 0.3327 対 0.3374 | **ほぼ互角。むしろ疎がわずかに上** |
| ハイブリッドは単独に勝つ | 0.3740 > 0.3374 / 0.3327 | この条件では成立(G-06 は L3 で正式判定) |
| 細かく刻むほど良い | 項単位 0.3327 対 条単位 0.4938 | **粗いほうが良い** |

**G-06 は破れることを許す。** 破れた場合、閾値を下げるのでも実装を疑うのでもなく、
**破れた条件を特定して出荷物にする**(F-09)。「ハイブリッドは常に勝つ」が通説であるため、
反例が取れるならそれ自体が教材である。ただし破れを主張する前に G-03 の陽性対照が緑であることを確かめる。

## 6. 品質基準

- 全ゲートの結果を**数値のまま**画面に出す。合否だけを出さない
- 苦手を隠さない。指標が悪い条件・当たらない質問類型は、数字ごと画面に出す(kototoi-do G-04 の規範)
- 「測って捨てた」ものを記録に残す。採用しなかった手法とその実測値を F-09 に書く

## 7. アーキテクチャ

| 層 | 実装 | 走る場所 |
|---|---|---|
| 採録・チャンク化・埋め込み・索引構築 | Node(`scripts/*.mjs`) | ローカル(事前計算) |
| 検索の参照実装 | Python(`harness/reference/`) | ローカル(G-04 の照合相手) |
| クエリ埋め込み | Transformers.js + onnxruntime-web(非 jsep) | 閲覧者の端末 |
| 検索・再ランク・評価の再計算 | TypeScript | 閲覧者の端末 |
| 配信 | Next.js `output: "export"` → Vercel 静的配信 | — |

### 7.1 kototoi-do から流用するもの

- 刈り込み済み multilingual-e5-small(29.8MB)。`query:` 接頭辞のトークンを名指しで残す手順を含む
- 非 jsep 版 onnxruntime-web(10.6MB)への絶対パス alias。client のみで分岐する
- int8 量子化: 次元ごとの中心 μ と幅 s を取り、s をクエリ側へ畳み込む(全体一律 127 倍にしない)
- `.vercelignore` の先頭を `/data` で固定する
- 本番検品は範囲つき GET で行う(Vercel は HEAD に content-length を返さない)

### 7.2 新規に踏み込むもの

- **条項参照オラクル**(§2.2)
- **文字 bi-gram BM25**。形態素解析器を積まない(kuromoji 辞書 15MB の回避)。転置索引は事前構築
- **つまみ × 指標の即時再計算**。分割戦略のみ索引を作り直すため 3〜4 通りに固定して事前計算し、
  必要になった一本だけ遅延読込する。それ以外のつまみは全て端末上で計算する

## 8. 画面

| ページ | 内容 |
|---|---|
| 引く | F-01 / F-07。デモの本体 |
| 刻む | F-02。分割戦略の実物比較 |
| 測る | F-04 / F-05 / F-06。つまみ × 指標 |
| 効いた・効かなかった | F-09 / F-11 |
| 作り方 | F-10 |

## 9. スコープ外

- **ブラウザ内 LLM(WebLLM 等)を積まない。** 1GB 超の追加ダウンロードと WebGPU 必須は N-04 と正面衝突する
- **BYOK(閲覧者の API キー)を既定の経路に置かない。** 本ループ群では実装しない
- **cross-encoder による再ランクを積まない。** 端末で走らせるには重い。MMR までとする
- **埋め込みモデルの差し替えを提供しない。** e5-small 一本に固定する(配布量の規律)
- **法令の解釈・法律相談を提供しない。** 本アプリは検索技術の実験台であり、
  回答は条文の抽出であって法的助言ではない旨を全ページに明示する
- 全文検索アプリとしての完成度は狙わない(それは aozora-sakuin の領分)
